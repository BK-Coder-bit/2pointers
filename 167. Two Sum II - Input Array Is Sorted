167. Two Sum II - Input Array Is Sorted

https://leetcode.com/problems/two-sum-ii-input-array-is-sorted/description/

class Solution(object):
    def twoSum(self, numbers, target): #timeO(n) sp:O(1)
        n=len(numbers)
        l=0
        r=n-1
        while(l<r):
            summ=numbers[l]+numbers[r]
            if(summ==target):
                return [l+1,r+1]
            elif summ<target:
                l+=1
            else:
                r-=1
